repos:
  - repo: local
    hooks:
      - id: task-check
        name: task check-fast (analyze + unit + widget)
        language: system
        entry: task check-fast
        pass_filenames: false
        always_run: true
